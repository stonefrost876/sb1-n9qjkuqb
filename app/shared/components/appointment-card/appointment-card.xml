<StackLayout class="card">
  <Label text="{{ clientName }}" class="font-semibold" />
  <Label text="{{ time }}" class="text-gray-600" />
  <Label text="{{ sessionType }}" class="text-gray-500" />
</StackLayout>